namespace ZoomIn.Properties
{

	/// <summary>
	/// This class allows you to handle specific events on the settings class.
	/// </summary>
	/// <remarks>
	/// The <see cref="SettingChanging"/> event is raised before a setting's value is changed.
	/// The <see cref="PropertyChanged"/> event is raised after a setting's value is changed.
	/// The <see cref="SettingsLoaded"/> event is raised after the setting values are loaded.
	/// The <see cref="SettingsSaving"/> event is raised before the setting values are saved.
	/// </remarks>
	internal sealed partial class Settings
	{
	}
}
